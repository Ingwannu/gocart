"use client";

const Loading = () => {
	return (
		<div className="flex items-center justify-center h-screen">
			<div className="w-11 h-11 rounded-full border-3 border-border border-t-accent animate-spin"></div>
		</div>
	);
};

export default Loading;
